<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="tot.dao.QnaDao">

<select id="getQnaDetail" parameterType="int" resultType="Qna">
	select * from qna
	where qnaid=#{qnaid}
</select>
 
<insert id="insertQna" parameterType="Qna">
	insert into qna(QNAID, memid, qna_001, qna_002, commentstatus, qnatitle, qnatext, QNAREGDATE, QNAUPDATE)
	values(seq_qnaid.nextval, #{memid},#{qna_001},'Q05', #{commentstatus},#{qnatitle}, #{qnatext}, SYSDATE, null)
</insert>

<select id="qnaList" resultType="Qna">
	select * 
	from qna 
		join member on qna.memid = member.memid
	where 
		1=1
</select>


</mapper>








