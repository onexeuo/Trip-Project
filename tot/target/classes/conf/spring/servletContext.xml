<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:context="http://www.springframework.org/schema/context"
       xmlns:mvc="http://www.springframework.org/schema/mvc"
       xsi:schemaLocation="http://www.springframework.org/schema/mvc https://www.springframework.org/schema/mvc/spring-mvc-4.3.xsd
                           http://www.springframework.org/schema/beans https://www.springframework.org/schema/beans/spring-beans-4.3.xsd
                           http://www.springframework.org/schema/context https://www.springframework.org/schema/context/spring-context-4.3.xsd">

    <context:component-scan base-package="tot" />
    
    <bean id="transactionManager" class="org.springframework.jdbc.datasource.DataSourceTransactionManager">
        <property name="dataSource" ref="dataSource" />
    </bean>
    
    <!-- Enable Spring MVC annotations -->
    <mvc:annotation-driven />
    
    <!-- ViewResolver -->
    <bean id="viewResolver" class="org.springframework.web.servlet.view.InternalResourceViewResolver">
        <property name="prefix" value="/jsp/" />
        <property name="suffix" value=".jsp" />
    </bean>
    
    <!-- MultipartResolver -->

	<bean class="org.springframework.web.multipart.commons.CommonsMultipartResolver" id="multipartResolver">
		<property value="104857600" name="maxUploadSize"/>
		<!-- 100MB -->
		<property value="10485760" name="maxUploadSizePerFile"/>
		<!-- 10MB -->
	</bean>
    
	<mvc:resources mapping="/static/**" location="/static/" />
	
	<bean class="org.mybatis.spring.mapper.MapperScannerConfigurer">
    	<property name="basePackage" value="tot.dao"/>
	</bean>

	<mvc:interceptors>
	    <mvc:interceptor>
	        <mvc:mapping path="/admin/**" /> <!-- Admin 경로에 대한 모든 요청을 인터셉트 -->
	        <mvc:exclude-mapping path="/admin/adminLogin" /> <!-- 로그인 페이지 요청 제외 -->
	        <mvc:exclude-mapping path="/admin/adminLoginProc" /> <!-- 로그인 처리 요청 제외 -->
	        <mvc:exclude-mapping path="/static/**" /> <!-- 정적 파일 요청 제외 -->
	        <bean class="tot.common.interceptor.AdminLoginInterceptor" /> <!-- 인터셉터 클래스 지정 -->
	    </mvc:interceptor>
	    
	</mvc:interceptors>

	
</beans>
